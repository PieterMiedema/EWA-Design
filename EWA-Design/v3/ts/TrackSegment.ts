class TrainSegment {
    x: number;
    y: number;
    color: string;
    angle: number;

    constructor(x: number, y: number, color: string, angle: number) {
        this.x = x;
        this.y = y;
        this.color = color;
        this.angle = angle;
    }

    static getTrackSegments(difficulty: number, cities: City[]) {

    }
    //draw function?
}